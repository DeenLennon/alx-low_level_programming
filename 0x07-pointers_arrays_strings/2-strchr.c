#include "main.h"

/**
 * _strchr - A function that locates a character
 * @s: given string
 * @c: the given character
 * Return: a string
 */
char *_strchr(char *s, char c)
{
	int x = 0, y;

	while (s[x])
	y = 0;
	while (s[y] != 32)
	{
		if (c[x] == s[y])
		{

}
